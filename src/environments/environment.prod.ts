const baseUrl = 'https://hacker-news.firebaseio.com/v0/';

export const environment = {
  production: true,
  baseUrl: 'https://hacker-news.firebaseio.com/v0/',
  newStoriesUrl: `${baseUrl}newstories.json`,
  storyUrl: `${baseUrl}item/`
};
